// Session 9 — Primitive Types & Variables
// CareOS Learning Curriculum

const staffName: string = "John Miller";       // string — name won't change
let roomNumber: number = 12;                   // number — residents can move rooms
let needsHelp: boolean = true;                 // boolean — status can change
let role: string | null = null;                // null — no role assigned yet
let medication: string | undefined = undefined; // undefined — unknown, may be set later
